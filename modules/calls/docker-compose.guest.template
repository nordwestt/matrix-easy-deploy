# Generated by apply.sh — guest lk-jwt-service extra_hosts for federated OpenID checks.
services:
  lk-jwt-service:
    extra_hosts:
      "{{GUEST_SERVER_NAME}}": "host-gateway"
      "{{GUEST_MATRIX_DOMAIN}}": "host-gateway"
