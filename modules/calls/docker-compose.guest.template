# Generated by apply.sh — guest stack extra_hosts for federated OpenID and room joins.
services:
  lk-jwt-service:
    extra_hosts:
      "{{GUEST_SERVER_NAME}}": "host-gateway"
  guest-tuwunel:
    extra_hosts:
{{GUEST_TUWUNEL_EXTRA_HOSTS}}
