# Generated by apply.sh — resolve guest/main Matrix hostnames to Caddy on caddy_net.
# Synapse and guest Tuwunel reach federation on :443 via the reverse proxy, not host-gateway.
services:
  caddy:
    networks:
      caddy_net:
        aliases:
{{GUEST_CADDY_NETWORK_ALIASES}}
